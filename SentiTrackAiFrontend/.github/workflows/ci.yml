name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npx tsc -b
      - run: npm run build
        env:
          # Placeholder — the build only needs this var to exist, not to be reachable.
          # Netlify's own build uses the real value from its site environment variables.
          VITE_API_ROOT_URL: https://api.example.com
